# Session: 2026-02-11

## Timeline
- **~8:00 AM**: Continued from compacted session (seed prompt: SEED-PROMPT-SCHEMA-FIX.md)
- **~8:10 AM**: Hydrated on key files (TestDatabaseInitializer, migrations 02/06/08/09/10)
- **~8:15 AM**: Phase 1 fix — edited TestDatabaseInitializer load order + rewrote migration 10 with email lookup
- **~8:18 AM**: First test run — 129/146 passed (up from 97). 17 FeatureEndpointTests still failing.
- **~8:22 AM**: Root cause: DevAdmin hardcoded ID (`019ac92a-...`) vs migration 02's random `uuid_generate_v7()` — `ON CONFLICT (email) DO NOTHING` silently skips the hardcoded ID insert
- **~8:25 AM**: Created `01a-seed-devadmin.sql` — seeds DevAdmin with hardcoded ID BEFORE migration 02
- **~8:27 AM**: **146/146 green.** Phase 1 complete.
- **~8:30 AM**: Updated 3 CLAUDE.md files (root, Tests, Infrastructure) via revise-claude-md skill
- **~8:32 AM**: Updated MEMORY.md + hot-state.md
- **~8:34 AM**: Committed + pushed to azure + origin (103 files, 13,409 lines)
- **~8:36 AM**: Deployed verification agent (Opus) to cross-check Phase 1 changes vs audit findings
- **~8:38 AM**: Wrote TASK-012 spec (Schema Retrofit + Auth Restore consolidated)
- **~8:39 AM**: Verification agent returned: **PASS with 2 non-blocking warnings**

## Objective
Execute Phase 1 of schema fix plan: unblock 49 failing integration tests caused by FK violations in migration 10 seed script. Document findings and plan Phase 2.

## What We Did

### 1. Phase 1 Schema Fix (COMPLETE)

Three changes, zero existing migrations modified:

| File | Change |
|------|--------|
| `01a-seed-devadmin.sql` (NEW) | Seeds DevAdmin with hardcoded ID before migration 02's random UUID |
| `10-feature-flag-seed.sql` (REWRITTEN) | `CROSS JOIN users WHERE email = 'admin@rgtspace.com'` for created_by/updated_by |
| `TestDatabaseInitializer.cs` (MODIFIED) | Load order: `00 → 01 → 03 → 01a → 02 → 06 → 08 → 09 → 10` |

**Key insight:** Migration 02 seeds admin with `uuid_generate_v7()` (random UUID each run). DevCurrentUser hardcodes `019ac92a-...`. The test helper `SeedDevAdminUserAsync` tried to insert the hardcoded ID but got silently skipped by `ON CONFLICT (email) DO NOTHING` because migration 02 already created that email with a different ID. Fix: seed the hardcoded ID BEFORE migration 02 runs.

### 2. CLAUDE.md Updates (3 files)
- Root: Added Migration Load Order + DevAdmin Contract sections
- Tests: Updated stale TestDatabaseInitializer RequiredFiles array
- Infrastructure: Fixed DevCurrentUser/CurrentUser active status (was wrong in docs)

### 3. Verification Agent (PASS)
- Cross-checked all 6 CRITICAL + 8 HIGH findings against Phase 1 changes
- All CRITICALs either fixed or deferred by design
- All HIGHs correctly scoped out to Phase 2/3
- No new issues introduced
- 2 non-blocking warnings: migration 10 silent no-op if admin missing, migration 02 module blocks not idempotent (pre-existing)

### 4. TASK-012 Spec Written
Consolidated auth restore + schema retrofit into single task:
- Phase A: Auth restore (12 endpoints + Extensions.cs)
- Phase B: 6 FK indexes, zombie-safe partial indexes, 9 triggers, timestamp standardization, DAC fix

## Test Results
**146/146 green** (97 unit + 49 integration)

## Commits
- `e6f3284` — feat: feature flag system (TASK-009/010/011) + schema audit + Phase 1 fix (103 files, +13,409 lines)

## Decisions Made
- Keep FK constraints on audit columns (37 across 13 tables) — Dapper safety net for hospital prototype
- Keep eval endpoints (`/features/evaluate`) as `AllowAnonymous` — frontend needs them pre-auth
- Migration naming: `01a` suffix for test-only DevAdmin seed (unconventional but functional)
